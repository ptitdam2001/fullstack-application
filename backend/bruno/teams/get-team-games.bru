meta {
  name: Get Team Games
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/team/{{teamId}}/games
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  page: 1
  limit: 20
}
