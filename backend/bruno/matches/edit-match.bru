meta {
  name: Edit Match
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/match/{{matchId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "PLAYED",
    "homeGoals": 2,
    "awayGoals": 1
  }
}
