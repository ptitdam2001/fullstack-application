meta {
  name: Update User
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/user/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "firstName": "Jane",
    "lastName": "Doe"
  }
}
