meta {
  name: Create Player
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/user/{{userId}}/player
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "jersey": 10,
    "position": "pivot"
  }
}
