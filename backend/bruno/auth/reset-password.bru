meta {
  name: Reset Password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "{{resetToken}}",
    "newPassword": "NewPassword1"
  }
}
