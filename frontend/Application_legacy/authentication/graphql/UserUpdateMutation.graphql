mutation UserUpdate($id: String!, $input: UserInput) {
  userUpdate(id: $id, inputs: $input) {
    user {
      id
      email
      name
      role
      avatar
      updatedAt
      createdAt
    }
  }
}
